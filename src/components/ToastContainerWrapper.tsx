"use client";

import React from "react";
import { ToastContainer } from "react-toastify";

const ToastContainerWrapper = (): JSX.Element => {
  return <ToastContainer />;
};

export default ToastContainerWrapper;
